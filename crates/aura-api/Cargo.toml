[package]
name = "aura-api"
version = "0.1.0"
edition = "2024"
description = "AURA REST API server for data retrieval and system monitoring"
license = "MIT"

[dependencies]
aura-core = { path = "../aura-core" }
aura-store = { path = "../aura-store" }