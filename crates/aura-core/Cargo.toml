[package]
name = "aura-core"
version = "0.1.0"
edition = "2024"
description = "Shared types, configuration, and error handling for AURA-EPICS"
license = "MIT"

[dependencies]
serde = { workspace = true }
toml = { workspace = true }
chrono = { workspace = true }
tracing-subscriber = { workspace = true }
serde_json = { workspace = true }
thiserror = "2.0.18"