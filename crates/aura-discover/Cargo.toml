[package]
name = "aura-discover"
version = "0.1.0"
edition = "2024"
description = "AURA IOC discovery, PV registry management, and auto-sharding"
license = "Apache-2.0"

[dependencies]
aura-core = { path = "../aura-core" }
aura-net = { path = "../aura-net" }
aura-store = { path = "../aura-store" }
