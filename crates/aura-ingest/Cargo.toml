[package]
name = "aura-ingest"
version = "0.1.0"
edition = "2024"
description = "AURA ingestion layer: PVA subscriber, epsilon-filter, heartbeat, Redis publisher"
license = "Apache-2.0"

[dependencies]
aura-core = { path = "../aura-core" }